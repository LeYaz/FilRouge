import { RechercheVPipe } from './recherche-v.pipe';

describe('RechercheVPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheVPipe();
    expect(pipe).toBeTruthy();
  });
});
